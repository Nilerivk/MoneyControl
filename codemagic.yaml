workflows:
  ios-workflow:
    name: MoneyControl iOS

    instance_type: mac_mini_m2

    environment:
      node: 22

    scripts:
      - npm ci
      - npm run build
      - npx cap sync ios

    artifacts:
      - ios/**/*.ipa
      - ios/**/*.app